#!/usr/bin/env bash
set -euo pipefail

cache_dir="${PWD}/.cache/rubocop_cache"
mkdir -p "$cache_dir"
export RUBOCOP_CACHE_ROOT="$cache_dir"

echo "pre-commit: running standardrb..."
if command -v asdf >/dev/null 2>&1; then
  asdf exec bundle exec standardrb
else
  bundle exec standardrb
fi
